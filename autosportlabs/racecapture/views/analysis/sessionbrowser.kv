<LapItemButton>:
    #background_down: ''
    font_size: sp(15)
    font_name: 'resource/fonts/ASL_light.ttf'

<Session>:
    orientation: 'vertical'
    BoxLayout:
        size_hint_y: 0.1
        orientation: 'horizontal'
        IconButton: #Edit button
            text: u'\uf044'
        IconButton: #Delete button
            text: u'\uf00d'
            color: ColorScheme.get_primary()
            on_press: root.prompt_delete_session()
    BoxLayout:
        size_hint_y: 0.9
        orientation: 'vertical'
        id: lap_list

<SessionBrowser>:
	orientation: 'vertical'
	
	
	