#:kivy 1.9.1
#:import ColorScheme autosportlabs.racecapture.theme.color.ColorScheme

<RaceTrackView>:
	orientation: 'vertical'
    TrackMap:
        track_color: ColorScheme.get_primary()
        id: trackmap
