#:kivy 1.8.0

<ChannelNameSelectorView>:
    BoxLayout:
        spacing: dp(5)
        orientation: 'horizontal'
        FieldLabel:
            text: 'Channel Name'
            halign: 'right'
        ChannelNameSpinner:
        	rcid: 'id'
            on_text: root.onSelect(*args)
        